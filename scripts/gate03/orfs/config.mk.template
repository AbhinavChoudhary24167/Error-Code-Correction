# Resolve @SOURCE_ROOT@ and @DESIGN_NAME@ into an external immutable run snapshot.
export PLATFORM = sky130hd
export DESIGN_NAME = @DESIGN_NAME@
export VERILOG_FILES = @VERILOG_FILES@
export SDC_FILE = @SOURCE_ROOT@/scripts/gate03/orfs/constraint.sdc
export CORE_UTILIZATION = 40
export CORE_ASPECT_RATIO = 1.0
export CORE_MARGIN = 10
export PLACE_DENSITY = 0.55
# The orchestration layer sets every seed exposed by the pinned ORFS revision to 42.

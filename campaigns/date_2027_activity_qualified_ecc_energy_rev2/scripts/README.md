# Scripts

- `build_artifacts.py`: verifies sealed inputs and regenerates data, tables, and
  all figure formats.
- `finalize_manifest.py`: records the retained evidence files and their SHA-256
  digests after an intentional package update.

Publication build and validation scripts are not part of the source repository.

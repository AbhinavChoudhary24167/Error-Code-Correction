# Scripts

- `build_artifacts.py`: verifies sealed inputs and regenerates data, tables, and
  all figure formats.
- `validate_package.py`: performs package, evidence, citation, figure, and PDF
  checks after compilation.
- `build_pdf.ps1`: reproducible PDF build entry point.
